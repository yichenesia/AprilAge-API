'use strict';

//import * as user from '../controllers/user2.controller.js';

const routes = (router) => {
  // router.route('/users')
  //   .get(user.users);
  // router.route('/create_user')
  //   .post(user.create_user);
  //   router.route('/user_info')
  //   .get(user.user_info);
};

export default routes;
